
export enum Point {
  x = 0,
  y = 1,
  velocity = 2,
  acceleration = 3,
  // color = 4,
  // colorString = 5,
}

export enum Color {
  r = 0,
  g = 1,
  b = 2,
  a = 3,
}

export enum Mouse {
  x = 0,
  y = 1,
  previousPos = 2,
}
